name: arg_parser
description: A schema-driven command-line parser with typed command inputs.
version: 1.0.0
homepage: https://github.com/louiss0/mamba
repository: https://github.com/louiss0/mamba
issue_tracker: https://github.com/louiss0/mamba/issues
topics:
  - command-line
  - parser
  - cli
  - schemas
  - yargs
executables:
  task_list: task_list
  yargs_task_list: yargs_task_list
  yargs_command_task_list: yargs_command_task_list
environment:
  sdk: ^3.12.2

dependencies:
  acanthis: ^1.6.0
  chalkdart: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
